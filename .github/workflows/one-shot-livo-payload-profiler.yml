name: one-shot-livo-payload-profiler

on:
  push:
    branches:
      - codex/add-compatibility-and-protocol-discovery-mode
    paths:
      - .github/livo_payload_profiler_patch.py
      - .github/workflows/one-shot-livo-payload-profiler.yml

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/add-compatibility-and-protocol-discovery-mode
          fetch-depth: 0

      - name: Apply implementation
        run: python3 .github/livo_payload_profiler_patch.py

      - name: Validate
        run: |
          git diff --check
          python3 -m py_compile components/gree_oem_boot_probe/__init__.py components/sinclair_ac/climate.py
          python3 -m unittest discover -s tests

      - name: Remove one-shot machinery
        run: |
          rm -f .github/livo_payload_profiler_patch.py
          rm -f .github/workflows/one-shot-livo-payload-profiler.yml

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Add full Livo payload evolution profiler"
          git push origin HEAD:codex/add-compatibility-and-protocol-discovery-mode
